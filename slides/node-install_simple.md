### NodeJS / instalación simple
<hr/>

* Windows: instalador .msi en https://nodejs.org/dist/latest/
* Ubuntu:
    ```shell
    sudo apt-get install nodejs
    ```
* Test en consola:
    ```shell
        node -v
    ```